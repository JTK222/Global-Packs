package net.dark_roleplay.gdarp.pack_finders;

import net.minecraft.server.packs.repository.Pack;
import net.minecraft.server.packs.repository.PackSource;
import net.minecraft.server.packs.repository.RepositorySource;

import java.util.function.Consumer;

public class ModLoadedPackFinder {//implements RepositorySource {
//	@Override
//	public void loadPacks(Consumer<Pack> packLoader, Pack.PackConstructor packBuilder) {
//		if(false){ //If mod is loaded, this is being coded in common code :harold:
//			Pack.create(
//				"Tintent Compat: Create", false,
//					() -> new PathPackResources("Tintent Compat: Create", getClass().getResource("compat_data_packs/create/").getPath()),
//					packBuilder, Pack.Position.TOP, PackSource.BUILT_IN
//			);
//		}
//	}
}
